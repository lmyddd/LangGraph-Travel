# travel-AI 面试学习指南（前端岗位）

> 按此路线学习，面试时能自信地讲清楚项目，并回答面试官的追问。

---

## 一、30秒项目简介（面试必问"介绍一下这个项目"）

> **模板**：这是一个**AI驱动的智能旅游规划助手**，前端用 Vue 3 + TypeScript + Vant 构建移动端 H5 应用，后端用 Express + LangChain 实现多 Agent 协作的 AI 行程规划。核心亮点是**实时流式可视化**——用户可以看到4个 AI Agent（查景点、查天气、规划行程、算预算）并行工作的全过程，每个 Agent 的状态变化通过 SSE 实时推送，前端用动态管道组件展示。支持用户认证、行程保存、AI 对话等功能。

---

## 二、学习路线（按天推进）

### 第 1 天：项目概览 + 技术栈

**目标**：能说清楚项目是什么、用了什么、为什么用。

| 要搞清楚的问题 | 去看什么文件 |
|---|---|
| 项目整体结构 | 根目录文件列表 |
| 用了哪些技术 | `travel-h5/package.json` |
| 前后端如何通信 | `travel-h5/src/utils/request.ts` |
| 路由是怎么设计的 | `travel-h5/src/router/index.ts` |
| 共享类型定义 | `shared/types.ts` |

**必须能回答**：
- ✅ "为什么选 Vue 3？" → Composition API 逻辑复用更好、TypeScript 支持更好、`<script setup>` 写起来更简洁
- ✅ "为什么用 Pinia 而不是 Vuex？" → Pinia 是 Vue 3 官方推荐、TypeScript 友好、不需要 mutations、支持持久化插件
- ✅ "为什么用 Vant？" → 面向移动端的 UI 库，组件丰富（Tabbar、Field、Collapse、Toast 等），按需引入体积小

---

### 第 2 天：认证系统 + 路由守卫

**目标**：理解 JWT 认证的前端实现。

| 要搞清楚的问题 | 去看什么文件 |
|---|---|
| 用户状态怎么管理 | `travel-h5/src/store/user.ts` |
| 路由守卫怎么写的 | `travel-h5/src/router/index.ts`（第 58-80 行） |
| 登录页面逻辑 | `travel-h5/src/views/login/index.vue` |
| JWT 怎么带上请求 | `travel-h5/src/utils/request.ts`（第 26-31 行拦截器） |
| Token 过期怎么处理 | `travel-h5/src/router/index.ts`（第 48-56 行） |

**必须能回答**：
- ✅ "JWT 存在哪里？为什么？" → localStorage，通过 pinia-plugin-persistedstate 自动持久化；不用 Cookie 因为不是 SSR 应用，不用 sessionStorage 因为需要跨标签页
- ✅ "Token 过期怎么处理？" → 路由 guard 里客户端解码 JWT payload 检查 `exp` 字段，过期则清除状态并跳转登录页
- ✅ "为什么有三个 Axios 实例？" → 不同 API 超时时间不同（auth 15s、travel 60s、trips 15s），分别封装更清晰

---

### 第 3 天：SSE 流式通信（核心亮点）

**目标**：这是面试最加分的点，必须深入理解。

| 要搞清楚的问题 | 去看什么文件 |
|---|---|
| SSE 怎么工作的 | `travel-h5/src/utils/request.ts`（第 98-154 行 fetchStream，第 183-262 行 fetchAgentStream） |
| Agent 状态怎么管理 | `travel-h5/src/composables/useAgentStream.ts` |
| 进度 UI 怎么渲染 | `travel-h5/src/components/AgentPipeline.vue` |
| 后台怎么推 SSE | `travel-server/src/utils/streamUtils.ts` |

**必须能回答**：
- ✅ "SSE 和 WebSocket 有什么区别？" → SSE 是**单向**的（服务器→客户端），基于 HTTP，自动重连，适合这个场景（AI 生成进度推送）；WebSocket 是双向的，更重，适合聊天类场景
- ✅ "怎么解析 SSE 数据？" → 用 `fetch()` + `ReadableStream.getReader()`，逐行读取 `data:` 前缀的 JSON 行
- ✅ "4 个 Agent 的状态怎么管理？" → 用一个 `Map<AgentName, AgentStateInfo>` 存在 composable 里，每个 SSE 事件更新对应 Agent，通过 `computed` 转为数组给组件渲染
- ✅ "SSE 失败了怎么办？" → 自动降级为普通 JSON POST 请求，保证功能不中断

---

### 第 4 天：核心页面 + 组件

**目标**：理解每个页面的功能和关键组件。

| 页面/组件 | 文件 | 需要关注的点 |
|---|---|---|
| 首页（表单） | `views/home/index.vue` | 表单验证、快捷城市选择、路由跳转带 query |
| 结果页 | `views/detail/index.vue` | AgentPipeline 加载状态、折叠面板、条件渲染 |
| AI 对话页 | `views/chat/index.vue` | 消息列表、SSE 流式输出、快捷问题 |
| 行程保存列表 | `views/history/index.vue` | 列表渲染、滑动删除 |
| Agent 管道组件 | `components/AgentPipeline.vue` | 动态状态样式、动画、连接线逻辑 |
| 景点卡片 | `components/SpotItem.vue` | 富信息展示、条件渲染 |
| 底部导航 | `App.vue` | Tabbar 路由联动、active 状态同步 |

**必须能回答**：
- ✅ "结果页的折叠面板怎么实现？" → 用 Vant 的 `van-collapse`，`v-model` 绑定 `activeDays` 数组控制展开项
- ✅ "表格数据怎么渲染？" → 每个 `DailyItinerary` 分上午/下午/晚上三个时段，用 `SpotItem` 组件复用渲染
- ✅ "首页表单验证做了哪些？" → 城市必选、预算≥100、天数 1-30、人数 1-50，验证通过后 `router.push` 带 query 跳转

---

### 第 5 天：性能优化 + 构建配置

**目标**：面试加分项，体现工程化意识。

| 要搞清楚的点 | 去看什么文件 |
|---|---|
| 路由懒加载 | `router/index.ts` → 所有组件都是 `() => import(...)` 动态导入 |
| 代码分包 | `vite.config.ts` → `manualChunks` 把 vant 和 vue-vendor 拆开 |
| 预压缩 | `vite.config.ts` → `vite-plugin-compression` 生成 .gz 和 .br |
| CSS 变量体系 | `style/common.css` |
| 动画实现 | `components/AgentPipeline.vue` 的 `@keyframes pulse` |
| Nginx 缓存策略 | `nginx.conf` → 静态资源1年缓存、index.html 不缓存 |

**必须能回答**：
- ✅ "做了哪些性能优化？" → ①路由懒加载减少首屏体积 ②manualChunks 拆分 vendor ③Gzip + Brotli 预压缩 ④CSS 变量统一主题 ⑤Nginx 静态资源强缓存
- ✅ "为什么手动拆 chunk？" → vant 体积大，单独拆出可以利用浏览器缓存，Vant 不常升级，缓存命中率高
- ✅ "预压缩和在线压缩有什么区别？" → 预压缩在构建时一次完成，Nginx 直接返回 .gz/.br 文件，零 CPU 开销；在线压缩每次请求都要压

---

### 第 6 天：模拟面试 + 可能的改进方向

**目标**：准备"如果你来做会怎么改进"的回答。

**可以提的改进方向**（展示你的思考深度）：

1. **虚拟列表**：行程列表如果很长可以用 `vue-virtual-scroller` 优化
2. **离线缓存**：用 Service Worker 做 PWA，首页和已加载的行程可离线查看
3. **单元测试**：给 composable 和 store 加 Vitest 测试
4. **错误监控**：接入 Sentry 捕获前端异常
5. **SSE 重连**：目前断开就降级 JSON，可加上指数退避重连
6. **骨架屏**：加载时用 Vant Skeleton 替代 loading 动画
7. **TypeScript 严格模式**：目前有一些 `any` 类型，可以收窄
8. **国际化**：目前是中文硬编码，可抽成 i18n

---

## 三、高频面试问题 & 回答要点

### Q1: "你在这个项目中最大的挑战是什么？"

> **建议回答**："挑战是 SSE 流式传输 + 多 Agent 状态可视化的前端实现。4 个 AI Agent 并行工作，从服务器推送不同的事件类型（agent_start / progress / complete / error），前端需要：
> 1. 用 fetch + ReadableStream 逐行解析 SSE 数据
> 2. 维护一个复杂的 Agent 状态机（idle → running → complete/error）
> 3. 用响应式数据驱动 UI 实时更新管道动画
> 4. SSE 失败时还要无缝降级到普通 JSON 请求
>
> 解决方式是把这部分逻辑抽成一个 composable（useAgentStream），返回干净的响应式状态给组件消费，组件只负责渲染。"

---

### Q2: "Composition API 和 Options API 你怎么选？为什么？"

> **建议回答**："这个项目全部用 Composition API + `<script setup>`。原因是：
> 1. 逻辑复用更好——比如 `useAgentStream` 抽成 composable，在多个组件可以复用
> 2. TypeScript 类型推断更自然，不需要 `this` 的类型体操
> 3. 代码组织更灵活——相关逻辑聚在一起，而不是分散在 data/methods/computed 里
> 4. `<script setup>` 写起来更简洁，不需要 `defineComponent` 和 `return`"

---

### Q3: "axios 拦截器做了什么？为什么搞多个实例？"

> **建议回答**："
> 1. **请求拦截器**：自动从 Pinia store 读取 JWT，加上 `Authorization: Bearer xxx` 头
> 2. **响应拦截器**：自动解包 `response.data`，让调用方直接拿到业务数据
> 3. **多实例原因**：不同 API 的超时时间不同——auth 接口 15s，travel（AI 生成）60s，trips CRUD 15s。分开管理语义更清晰，也方便后续对不同实例加不同的中间件"

---

### Q4: "Pinia 和 Vuex 的区别？"

> **建议回答**："
> 1. Pinia 不需要 mutations——直接改 state，更简洁
> 2. TypeScript 支持更好——不需要额外的类型声明
> 3. 支持 Composition API 风格的 store 定义（`defineStore` 里直接写 `ref`/`computed`）
> 4. 天然支持持久化插件——本项目的用户 token 和用户信息通过 `pinia-plugin-persistedstate` 自动存 localStorage
> 5. 体积更小，tree-shaking 友好"

---

### Q5: "路由守卫做了什么？"

> **建议回答**："
> 1. 进入每个路由前检查 token 是否过期——客户端解码 JWT payload 的 `exp` 字段
> 2. 过期则清除登录状态
> 3. 未登录用户访问任何非 `/login` 页面→重定向到 `/login`，并带 `?redirect=` 参数用于登录后跳回
> 4. 已登录用户访问 `/login`→重定向到首页
> 5. 所有路由组件都是懒加载的（`() => import(...)`），优化首屏速度"

---

### Q6: "这个项目里的 TypeScript 怎么用的？"

> **建议回答**："
> 1. **共享类型**：`shared/types.ts` 定义了前后端共用的接口（User、TripPlan、DailyItinerary、AgentEvent 等），保证 API 契约一致
> 2. **Props 类型**：组件用 `defineProps<{ ... }>()` 做类型约束
> 3. **Composable 返回类型**：显式声明 `UseAgentStreamReturn` 接口
> 4. **API 泛型**：`authPost<T>(...)` 让调用方指定返回类型
> 5. **联合类型**：Agent 事件用 Union Type（`AgentEvent = {type: 'agent_start'} | {type: 'agent_progress'} | ...`），配合 switch 有穷尽性检查"

---

### Q7: "如果让你把这个项目做得更好，你会怎么做？"

> 见上面"第 6 天"的改进方向列表，挑 2-3 个详细说。

---

## 四、白板画图准备

面试官可能让你画架构图。准备好画出：

```
┌─────────────────────────────────────────────────────┐
│  Vue 3 SPA (travel-h5)                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ Vue Router│  │  Pinia   │  │  Composables     │  │
│  │ (懒加载)   │  │ (user)   │  │ (useAgentStream) │  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
│  ┌──────────────────────────────────────────────┐   │
│  │  Vant UI 组件库 (Tabbar/Collapse/Field/...)  │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │  HTTP Layer                                  │   │
│  │  ┌─────────┐  ┌──────────┐  ┌────────────┐  │   │
│  │  │ axios   │  │ axios    │  │ fetch +     │  │   │
│  │  │ /auth   │  │ /travel  │  │ ReadableStr │  │   │
│  │  │ 15s     │  │ 60s      │  │ eam (SSE)   │  │   │
│  │  └─────────┘  └──────────┘  └────────────┘  │   │
│  └──────────────────────────────────────────────┘   │
└──────────────────┬──────────────────────────────────┘
                   │ JWT + REST / SSE
┌──────────────────▼──────────────────────────────────┐
│  Express Server (travel-server)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ Auth     │  │ Travel   │  │ Trips CRUD       │  │
│  │ Routes   │  │ Routes   │  │ Routes           │  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
│  ┌──────────────────────────────────────────────┐   │
│  │  LangGraph AI Pipeline                       │   │
│  │  Researcher → Weather → Planner → Reviewer   │   │
│  │      (Tavily)  (OpenMetro) (LLM+Tools)       │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │  Prisma + PostgreSQL                          │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

---

## 五、学习检查清单

面试前确保能做到：

- [ ] 不看代码能说出项目的完整技术栈
- [ ] 能解释清楚 SSE 从前端到后端的完整链路
- [ ] 能说明 JWT 认证从前端到后端的完整流程
- [ ] 能说清每个页面/组件的职责和关键逻辑
- [ ] 能讲出至少 3 个技术难点和解决方案
- [ ] 能提出至少 3 个改进方向
- [ ] 能手画架构图
- [ ] 能解释为什么选择 Vue 3 / Pinia / TypeScript / Vant
- [ ] 能把关键代码片段（路由守卫、Axios 拦截器、SSE 解析）的伪代码写出来
- [ ] 准备好"你还有什么想问的"的反问（团队技术栈、项目规模、开发流程等）

---

## 六、常见追问陷阱

1. **"这个项目是你自己做的吗？"** → 诚实回答，但要能说清楚每部分的功能和原理
2. **"有没有遇到过什么 bug？怎么解决的？"** → 可以说 SSE 数据解析的编码问题（`TextDecoder` 的 `stream: true`）、跨行数据被截断的问题
3. **"这个项目上线了吗？"** → 如实说，但可以说准备了完整的 Nginx 生产配置（`nginx.conf`）
4. **"你负责哪部分？"** → 如实说，但展示你对全栈的理解
5. **"后端 AI pipeline 你理解吗？"** → 作为前端能说出 4 个 Agent 的分工和工作流程就很加分

---

## 七、可做的项目增强（提高含金量）

如果你想在面试前再花 1-2 天让项目更有说服力：

1. **加一个搜索防抖**：城市输入加 `debounce`，体现前端基础功
2. **加加载骨架屏**：用 Vant Skeleton 替代文字 loading
3. **加错误重试**：SSE 断开后显示"重试"按钮
4. **补 TypeScript 严格模式**：把 `any` 改成具体类型
5. **加 `v-memo` 优化列表渲染**：在 SpotItem 列表上加 memo 减少重渲染
6. **写一个 README**：加上项目截图、技术栈说明、本地运行步骤
